import 'package:flutter/material.dart';

class DryingScreen extends StatelessWidget {
  const DryingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("🔥 Séchage à venir"),
    );
  }
}
