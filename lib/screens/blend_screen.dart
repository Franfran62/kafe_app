import 'package:flutter/material.dart';

class BlendScreen extends StatelessWidget {
  const BlendScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("🧪 Assemblage à venir"),
    );
  }
}
